/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["https://links.papareact.com/", "fakestoreapi.com", "i.dummyjson.com"]
    }
}

module.exports = nextConfig
